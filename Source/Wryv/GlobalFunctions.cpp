#include "Wryv.h"
#include "GlobalFunctions.h"

DEFINE_LOG_CATEGORY( K );
//DEFINE_LOG_CATEGORY_STATIC(K, Log, All); // when log category is for a single file (declared in .cpp)

FVector UnitX(1.f,0.f,0.f), UnitY(0.f,1.f,0.f), UnitZ(0.f,0.f,1.f);