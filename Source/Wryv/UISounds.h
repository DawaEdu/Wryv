#pragma once

#include "UISounds.generated.h"

UENUM()
enum UISounds
{
  BuildingPlaced = 0  UMETA(DisplayName = "BuildingPlaced"),
  Error               UMETA(DisplayName = "Error"),
  Click               UMETA(DisplayName = "Click"),
  Music               UMETA(DisplayName = "Music"),
};
